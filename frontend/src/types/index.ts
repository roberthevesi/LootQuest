export interface FindingItem {
    id: number;
    name: string;
    location: string;
    owner: string;
  }
  
  export interface LostItem {
    id: number;
    name: string;
    location: string;
  }